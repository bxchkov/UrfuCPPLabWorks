/*
Лабораторная работа 3
Задание 1
Программа сортировки методом выбора (Selection Sort)
(в задании названо "методом минимального и максимального", но реализован метод выбора минимума)
*/

#include <iostream>
#include <windows.h>

using namespace std;

int main() {
    SetConsoleOutputCP(CP_UTF8); // Для нормального отображения русского языка в консоли

    int arr[] = {-2, 5, -8, 1, -10, 2, 11, 0, 4, -9};

    int iMin; // Индекс минимального элемента в текущей неотсортированной части
    int arrLength = sizeof(arr) / sizeof(int); // Вычисление длины массива (общий размер в байтах / размер одного элемента)

    cout << "Исходный массив: ";
    for (int i = 0; i < arrLength; i++) {
        cout << arr[i] << ' ';
    }
    cout << endl;

    // Сортировка методом выбора (Selection Sort)
    // Идея: на каждом шаге i находим минимальный элемент в подмассиве от i до конца
    // и меняем его местами с элементом на позиции i.
    for (int i = 0; i < arrLength - 1; i++) {
        iMin = i; // Предполагаем, что текущий элемент i - минимальный

        // Ищем реальный минимум в оставшейся части массива
        for (int j = i + 1; j < arrLength; j++) {
            if (arr[j] < arr[iMin]) {
                iMin = j; // Запоминаем индекс нового минимума
            }
        }

        // Обмен элементов: ставим найденный минимум на позицию i
        if (iMin != i) { // Оптимизация: меняем только если минимум не на своем месте
            int t = arr[i];
            arr[i] = arr[iMin];
            arr[iMin] = t;
        }
    }

    cout << "Отсортированный массив: ";
    for (int i = 0; i < arrLength; i++) { // Вывод отсортированного массива
        cout << arr[i] << ' ';
    }
    cout << endl;

    return 0;
}
